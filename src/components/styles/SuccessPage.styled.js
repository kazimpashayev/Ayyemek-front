import styled from 'styled-components';

export const StyledSuccessPage = styled.div`
  .info label {
    display: block;
    font-size: 24px;
    margin-bottom: 10px;
  }

  .info span {
    color: var(--accent-color);
    font-size: 22px;
  }
`;
